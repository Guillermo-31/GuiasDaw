let num, cantcifras, cantim, cantpar, sumim, sumpar, sumtotal, ciframayor, ciframenor, cifras = new Array;

num = prompt('Ingrese el número entero que quiera: ','');

for (let i = 0; i <= 9; i++) {
  if (cantcifras == num){
    
  }
}